version: '3.8'

services:
  mcp-server:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      platforms:
        - linux/amd64
    platform: linux/amd64
    container_name: mcp-server
    ports:
      - "8400:8400"
    restart: unless-stopped

